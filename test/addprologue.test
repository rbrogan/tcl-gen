package require tcltest
package require -exact gen 1.8.0

namespace eval AddPrologueTest {}



if {([llength $::argv] == 0) || ([lindex $::argv 0] ne "target")} {
} else {
     foreach Element [lrange $::argv 1 end] {
          AddPrologueTest::Test-[set Element]
     }
}

